#include "main.h"
/**
 * print_most_numbers - to print numbers 0 through 9
 *
 * Return: Always 0 (Success)
 */
void print_most_numbers(void)
{
	int i;

	for (i = 48; i <= 57)  || (i != 50 && != 52)
	{
		_putchar(i);
		i++;
	}
	_putchar('\n');
}
